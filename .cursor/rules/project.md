# このワークスペース = 外浦マップ（sotoura-map）

- **リポジトリ**: `Stand-koike/sotoura-map`（本フォルダ）
- **編集対象**: `web/`（外浦設定）, `clients/sotoura/`
- **触らない**: `shimoda-map` / 下田、`web/100.png` 系（本リポに存在しない）
- **確認**: `web/config.js` の `APP_TITLE` は「外浦マップ」
- **ブランチ**: 通常は `main` のみ
- **シート未設定**: `MAPBOX` のみで地図確認可。`GOOGLE_SHEET_ID` は後日 secrets 追加

Agent 依頼の冒頭に案件名を書くこと:

```
【案件】外浦 / リポ sotoura-map / main
```
